------------video.cpp----------------------------
//Setto il colore del testo e quello dello sfondo
setTextColor(White);
setBackColor(Black);
è inutile è la situazione di default

Possibile modifica alla funzione GotoXY(unsigned int x, unsigned int y):
	- Da modificare in Video::GotoXY(int x, int y); Perchè in caso di valori negativi vengono interpretati come relativi alla posizione corrente.

La funzione gotoxy non funziona correttamente

------------stringutil.cpp-----------------------

dec non è necessario scrivere nulla
oct si scrive con uno 0 davanti
hex si scrive con 0x davanti